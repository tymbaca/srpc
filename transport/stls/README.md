# sTLS - stupid Transport Layer Security

sTLS is sRPC transport layer providing key exchange and symmetric encription
of transfered data. Currently it uses `crypto/ecdh` Diffie-Hellman key exchange
implementation.
